# Copy to .env (gitignored) and adjust.
# Vite exposes any var prefixed with VITE_ to the client.

# Where the backend lives. Leave blank to use the same origin (production with
# reverse proxy) or "/api" for the Vite dev proxy.
VITE_API_URL=
